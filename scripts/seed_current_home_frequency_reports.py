from __future__ import annotations


def main() -> None:
    raise SystemExit(
        "seed_current_home_frequency_reports.py used the removed SQLite local store. "
        "Seed or import history through the MySQL application storage workflow instead."
    )


if __name__ == "__main__":
    main()
